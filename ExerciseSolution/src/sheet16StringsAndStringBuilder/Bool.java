package sheet16StringsAndStringBuilder;

public class Bool {

	public static void main(String[] args) {
		
		boolean isFound = false;
		
		if(isFound == true)
			System.out.println(isFound);
	
		if(!isFound)
			System.out.println("if isFound is false then the not of false is true"
					+ ", so !isFound = "+!isFound);
	}
}
